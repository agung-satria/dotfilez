#!/bin/sh

# The clickable help menu. Middle click to restart wm.

    case $BLOCK_BUTTON in
      1) notify-send "❓ Help module" "\- Press Win+Shift+B to toggle systray." ;;
      6) "$TERMINAL" -e "$EDITOR" "$0" ;;
    esac; echo "  "
